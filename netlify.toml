[build]
  command   = "npm run build && node scripts/bundle-lambda.mjs"
  publish   = "build"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from   = "/*"
  to     = "/.netlify/functions/sveltekit-render"
  status = 200
  force  = false